name: My First Wokflow

on: push

jobs:
    first_job:
        runs-on: ubuntu-latest
        steps:
        - name: Checkout Repository
          uses: actions/checkout@v4

        - name: welcome message
          run: echo "My First Githiub Actions Job"

        - name: List Files
          run: ls

        - name: Read File
          run: cat README.md